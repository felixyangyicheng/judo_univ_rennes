@page "/logout"
@using judo_univ_rennes.Contracts
@inject IAuthRepo _authRepo
@inject NavigationManager _navManager

@code {
    protected override async Task OnInitializedAsync()
    {
        await _authRepo.Logout();
        _navManager.NavigateTo("index");
        StateHasChanged();
    }
}