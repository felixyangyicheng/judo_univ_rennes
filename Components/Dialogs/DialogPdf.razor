@using BlazorPro.Spinkit
<MudDialog DisableSidePadding="true">
    <DialogContent>

            @if (Loading == true)
            {
                <Bounce></Bounce>
            }
            else
            {
            if (Loading == true)
            {
                <Bounce></Bounce>
            }
                <MudText Style="white-space: pre-wrap;">@fileName</MudText>
            <iframe src="@pdfUrl" class="w-100 iframePdf"
                style="max-width:100vw;"></iframe>

            }
     
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Ok">OK</MudButton>
    </DialogActions>
</MudDialog>