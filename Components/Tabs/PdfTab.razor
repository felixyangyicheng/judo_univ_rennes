@page "/pdfview/{filename}"

            @if (loading == true)
            {
                <Bounce></Bounce>
            }
            else
            {
    if (loading == true)
            {
                <Bounce></Bounce>
            }
                <MudText Style="white-space: pre-wrap;">@fileName</MudText>
    <iframe src="@pdfLink" class="w-100 iframePdf"
                style="max-width:100vw;"></iframe>
                <MudIconButton Icon="@Icons.Material.Filled.KeyboardReturn" OnClick="Back"></MudIconButton>
            }